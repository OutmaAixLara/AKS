apiVersion: v1
kind: Service
metadata:
  labels:
    app: helloaachen
  name: helloaachen-v1-np
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: helloaachen
  type: NodePort